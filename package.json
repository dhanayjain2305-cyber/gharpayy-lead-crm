{
  "name": "gharpayy-crm",
  "version": "1.0.0",
  "description": "Gharpayy Lead Management System - Full-stack SaaS CRM",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:frontend": "cd frontend && npm install && npm run dev",
    "dev:backend": "cd backend && npm install && node server.js",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && node server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
